[project]
name = "mouse-ar"
version = "0.1.0"
description = "Phase 1 CV tracking: glue a crosshair to a mouse nose at >=25fps"
requires-python = ">=3.10"
dependencies = [
    "numpy>=1.24",
    "opencv-python-headless>=4.8",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
